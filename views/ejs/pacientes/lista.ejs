<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
</head>

<style>
:root{
    --blanco: rgb(255, 255, 255);
    --negro: rgb(34, 34, 34);
    --magenta: rgb(149, 0, 154);
    --medianoche: rgb(18, 13, 65);
  }

  body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: var(--blanco);
    color: var(--negro);
    margin: 0;
    padding: 40px 20px;
    line-height: 1.6;
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 100vh;
    box-sizing: border-box;
  }

  h1 {
    color: var(--medianoche);
    border-bottom: 3px solid var(--magenta);
    padding-bottom: 10px;
    margin-bottom: 25px;
    text-align: center;
    width: 100%;
    max-width: 600px;
  }

  a{
    text-decoration: none;
    color: var(--magenta);
  }

  li{
    list-style: number;
  }
</style>

<body>
  <h1><%= title %></h1>
  <a href="/pacientes/nuevo">Crear nuevo paciente</a>
  <ul>
    <% pacientes.forEach(p => { %>
      <li>
        <%= p.nombre %> | <%= p.email %>
        <a href="/pacientes/eliminar/<%= p.id %>">Eliminar</a>
      </li>
    <% }) %>
  </ul>
</body>
</html>
